const config = {
    fps: 60,
    // Specifically 123 BPM
    gravity: 2.81,
    jumpForce: 0.70,
    scrollSpeed: 0.1,
    playerMaxRotChange: 1000,
};

const keybinds = {
    jump: ["ArrowUp", " ", "w"],
    quit: ["Escape"],
};