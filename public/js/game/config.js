const config = {
    fps: 60,
    // Specifically 123 BPM
    gravity: 2.9,
    jumpForce: 0.75,
    scrollSpeed: 0.1,
    playerMaxRotChange: 1000,
};

const keybinds = {
    jump: ["ArrowUp", " ", "w"],
    quit: ["Escape"],
};