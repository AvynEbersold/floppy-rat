const config = {
    fps: 60,
    gravity: 1.2,
    jumpForce: 0.45,
    scrollSpeed: 0.1,
    playerMaxRotChange: 0.3,
};

const keybinds = {
    jump: ["ArrowUp", " ", "w"],
    quit: ["Escape"],
};
